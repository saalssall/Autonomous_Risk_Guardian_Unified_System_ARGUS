# Copy to .env.local for local overrides (not committed), or set
# VITE_BACKEND_URL directly in Vercel's project settings for deployment.
VITE_BACKEND_URL=http://localhost:8000
VITE_STREAM_URL=http://<pi-ip>:8081/stream
