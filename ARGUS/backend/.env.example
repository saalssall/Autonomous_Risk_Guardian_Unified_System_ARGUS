# Copy to .env and fill in, or export directly in your shell before running uvicorn.
# Get a key at https://console.anthropic.com — required for POST /api/ai-explanation/{node_id}.
# Nothing else in the backend needs this; the rest of the app runs fine without it.
ANTHROPIC_API_KEY=
