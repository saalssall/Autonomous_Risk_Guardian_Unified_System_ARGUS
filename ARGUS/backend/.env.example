# Copy to .env and fill in, or export directly in your shell before running uvicorn.
# Get a FREE key (no credit card required) at https://aistudio.google.com/apikey
# — required for POST /api/ai-explanation/{node_id}. Nothing else in the
# backend needs this; the rest of the app runs fine without it.
GEMINI_API_KEY=
